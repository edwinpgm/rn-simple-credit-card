import {
  StyleSheet,
} from "react-native";


const styles = StyleSheet.create({
  CCInput_label: {
    paddingBottom: 5,
    fontSize: 12,
    fontWeight: "700",
    textTransform: "uppercase",
  },
  CCInput_input: {
    padding: 10,
    borderColor: "#e0e0e0",
    borderRadius: 4,
    borderWidth: 1,
  }
});

export default styles;